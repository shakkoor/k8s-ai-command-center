apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-backend.fullname" . }}
  labels:
    {{- include "flask-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-backend.selectorLabels" . | nindent 4 }}
